apiVersion: 1

providers:
  - name: 'Prometheus'
    # orgId: 1
    # folder: 'General'
    type: file
    disableDeletion: false
    editable: true
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
